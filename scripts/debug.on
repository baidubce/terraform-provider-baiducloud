# Usage: source debug.on
# Use the function addDebug to print the debug log. The log is saved in the file which is configured by the TF_LOG_PATH environment variable. Such as terraform.log.
# 使用addDebug方法打印调试日志，日志保存在TF_LOG_PATH环境变量配置的文件中，如terraform.log。
export TF_LOG_PATH=terraform.log
export TF_LOG=TRACE
export DEBUG=terraform
